# NGF Validation Pages

This directory contains legacy NGF framework validation pages only. It is outside the ASF Workshop product navigation and must not be registered as a product entry, shortcut, widget action, or external launch target.

The product compatibility path at `pages/ngf/MainMenuPage` renders `SteamArkHomePage`; product UI changes belong under `pages/steamArk`.
